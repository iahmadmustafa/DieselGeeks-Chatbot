# WooCommerce REST API (read-only key)
WOOCOMMERCE_URL=https://dieselgeeks.com.au
WOOCOMMERCE_CONSUMER_KEY=
WOOCOMMERCE_CONSUMER_SECRET=

# Upstash Redis
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Sync endpoint protection (Vercel cron sends Authorization: Bearer <CRON_SECRET>)
CRON_SECRET=

# OpenAI — fitment parse fallback + chat assistant
OPENAI_API_KEY=
FITMENT_LLM_MODEL=gpt-5-mini
FITMENT_LLM_MAX_OUTPUT_TOKENS=2000
FITMENT_LLM_REASONING_EFFORT=low
FITMENT_LLM_CONCURRENCY=8

# Chat assistant (Phase 2 + Phase 3 guardrails)
CHAT_MODEL=gpt-5-mini
CHAT_MAX_OUTPUT_TOKENS=800
CHAT_MAX_MESSAGE_LENGTH=500
CHAT_MAX_HISTORY_MESSAGES=16
CHAT_RATE_LIMIT_IP_PER_MIN=12
CHAT_RATE_LIMIT_SESSION_PER_DAY=60
CHAT_DAILY_BUDGET_USD=5
CHAT_ESTIMATED_COST_PER_REQUEST_USD=0.02
CONTACT_URL=https://dieselgeeks.com.au/contact-us/

# CORS — production: store domains only (no localhost).
# Local dev auto-allows localhost even if omitted here.
ALLOWED_ORIGINS=https://dieselgeeks.com.au,https://stage2.dieselgeeks.com.au
